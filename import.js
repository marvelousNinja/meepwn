
// db.createCollection('projects');
// db.createCollection('directories');
// db.createCollection('files');

// db.projects.insert({name: 'TestProject'});
// testProject = db.projects.findOne({name: 'TestProject'});

// db.directories.insert({name: 'TestProjectRoot'});
// testProjectRoot = db.directories.findOne({name: 'TestProjectRoot'});

// db.projects.udpate({_id: testProject._id}, {rootProjectId: testProjectRoot._id});

// db.directories.insert({name: 'Sibling1', parentId: testProjectRoot._id});
// db.directories.insert({name: 'Sibling2', parentId: testProjectRoot._id});
// db.directories.insert({name: 'Sibling3', parentId: testProjectRoot._id});
